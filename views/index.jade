doctype html
html(lang='en', ng-app="biddingApp")
    head
        meta(charset="utf-8")
        title Bidding App!
        script(src="https://cdnjs.cloudflare.com/ajax/libs/angular.js/1.4.3/angular.js")
        script(src="https://cdnjs.cloudflare.com/ajax/libs/angular.js/1.4.3/angular-route.js")
        
        // Load the models so we can access in Angular script
        script(src="/models.js")
        
        // Socket.io generates this file on the client for you
        // Then we initialize the socket
        script(src="/socket.io/socket.io.js")
        script(src="/socket-events.js")
        
        // Load our Angular script
        script(src="/index.js")

    body
        h1 Hello!

        div(ng-view)